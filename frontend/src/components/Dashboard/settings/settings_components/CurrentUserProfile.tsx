import React from 'react';
import styled from 'styled-components';

/*
    This component should recieve detailed props from parent--which is the UserSettings view, that would render the current profile picture, and current user details save for the password.
*/

const CurrentUserProfile = () => {
    return <div></div>;
};

export default CurrentUserProfile;
